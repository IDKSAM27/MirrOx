[package]
name = "mirrox"
version = "0.1.0"
edition = "2021"

[dependencies]
crossbeam-channel = "0.5"
ffmpeg-next = "7.1.0"
ffmpeg-sys-next = "7.1.0"
sdl2 = { version = "0.35.2", features = ["bundled"] }

[build-dependencies]
cc = "1.0"

[features]
default = []
